{
  "mcpServers": {
    "panda-server": {
      "command": "python3",
      "args": ["${CLAUDE_PLUGIN_ROOT}/servers/panda-server.py"],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      }
    }
  }
}
